function coefs = ComputeTaylorNthCoefTanh(n)

coefs = zeros(size(n));
odds = mod(n, 2) == 1;
coefs(odds) = ...
    (2.^(n(odds)+1) - 1) ...
    .*2.^(n(odds)+1) ...
    .*bernoulli(n(odds)+1) ...
    ./ factorial(n(odds)+1);

end
